<!DOCTYPE html>
<html>
<head>
	<meta charset="EUC-KR">
	<title>Insert title here</title>
</head>
<body>
	<h1>함수의 표현식</h1>
	<h2> 
		변수 = function 함수명(){<br/>
			//실행할 명령문<br/>
		}<br/>
	
		위와 같이 함수를 표현식으로 정의하고 사용하면 함수의 호이스팅이 일어나지 않는다.
		또한 =연산자를 이용해서 함수를 변수에 할당
		이런 경우 정의한 함수명을 사용하지 않고 변수명을 함수명 처럼 이용한다.
	</h2>

	<script type="text/javascript">
		//function test()를 myfunc로 정의하겠다 라는 의미이다
		//아래와 같이 정의해놓으면 더이상 함수 호이스팅이 일어나지 않는다.
		//myfunc(90);
		myfunc = function test(num){
			if(num>90){
				alert("성공");			
			} else{
				alert("실패");
			}
		}
		myfunc2 = function(num){//익명으로 정의할 수 있다.
			if(num>90){
				alert("성공");			
			} else{
				alert("실패");
			}
		}
		
		
		//test(90); - 표현식으로 함수를 정의했기 때문에 함수명을 
		//사용할 수 없고 변수명을 이용해야 한다.
		//변수를 함수처럼 사용할 것이다.
		myfunc(90);
		//어짜피 변수명으로 불릴거기 때문에 함수 이름을 정의해 줄 필요가 없다.
		myfunc2(90);
	</script>
</body>
</html>